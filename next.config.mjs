/**
 * Auto-generated ESM config to replace next.config.ts for runtime.
 * Kept minimal to match the original TypeScript config.
 */
const nextConfig = {
  /* config options here */
};

export default nextConfig;
